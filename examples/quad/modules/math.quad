func add(a: intg, b: intg) -> intg:
    back a + b

func factorial(n: intg) -> intg:
    cell res: intg := 1
    cell k: intg := n
    loop k > 1:
        res := res * k
        k := k - 1
    back res
