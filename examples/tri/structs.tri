typ Usr {
    nam: txt,
    age: int
}

imp Usr:
    def new(n: txt, a: int) -> Usr:
        var u: Usr := Usr{ nam: n, age: a }
        ret u

    def inc(slf) -> vod:
        slf.age := slf.age + 1

def main() -> int:
    var u: Usr := Usr.new("sato", 31)
    u.inc()
    prn(u.age)
    ret u.age
